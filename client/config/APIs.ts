const APIs = {
    pages: {
        getAll: "",
        getByUser: "/page/user/${userId}",
    },
    project: {
        getAll: "",
    },
};

export const apiVariables = {
    userId: "${userId}",
};

export default APIs;
